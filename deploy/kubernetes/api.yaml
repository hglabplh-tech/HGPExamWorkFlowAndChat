apiVersion: apps/v1
kind: Deployment
metadata:
  name: study-harbour-api
  labels: { app.kubernetes.io/name: study-harbour, app.kubernetes.io/component: api }
spec:
  replicas: 1
  selector:
    matchLabels: { app.kubernetes.io/name: study-harbour, app.kubernetes.io/component: api }
  template:
    metadata:
      labels: { app.kubernetes.io/name: study-harbour, app.kubernetes.io/component: api }
    spec:
      securityContext:
        seccompProfile: { type: RuntimeDefault }
      containers:
        - name: api
          image: ghcr.io/OWNER/study-harbour:0.1.0
          imagePullPolicy: IfNotPresent
          ports:
            - { name: http, containerPort: 8000 }
          envFrom:
            - configMapRef: { name: study-harbour }
            - secretRef: { name: study-harbour }
          readinessProbe:
            httpGet: { path: /ready, port: http }
            initialDelaySeconds: 15
            periodSeconds: 10
          livenessProbe:
            httpGet: { path: /health, port: http }
            initialDelaySeconds: 30
            periodSeconds: 20
          resources:
            requests: { cpu: 500m, memory: 1Gi }
            limits: { cpu: "2", memory: 4Gi }
          securityContext:
            allowPrivilegeEscalation: false
            capabilities: { drop: [ALL] }
            readOnlyRootFilesystem: true
            runAsNonRoot: true
          volumeMounts:
            - { name: tmp, mountPath: /tmp }
            - { name: model-cache, mountPath: /app/.cache }
      volumes:
        - name: tmp
          emptyDir: {}
        - name: model-cache
          emptyDir: {}
---
apiVersion: v1
kind: Service
metadata:
  name: study-harbour-api
spec:
  selector: { app.kubernetes.io/name: study-harbour, app.kubernetes.io/component: api }
  ports:
    - { name: http, port: 80, targetPort: http }

